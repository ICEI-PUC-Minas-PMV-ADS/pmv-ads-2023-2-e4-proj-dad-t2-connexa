import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
      width: '100%',
      marginVertical: 10,
      borderRadius: 6,
    },
    text: {
      fontWeight: 'bold',
      fontSize: 15,
      lineHeight: 28,
    },
  });
  
export default styles;