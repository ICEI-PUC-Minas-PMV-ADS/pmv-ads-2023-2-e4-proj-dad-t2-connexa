export default class ListItemDTO {
    constructor(id, listaId, nome, status, descricao) {
        this.Id = id,
        this.ListaId = listaId,
        this.Descricao = descricao,
        this.Nome = nome,
        this.Status = sttaus
    }
}