export default class NewUserListaDTO {
    constructor(listaId, userId, userListaRole, userListaStatus) {
        this.listaId = listaId;
        this.userId = userId;
        this.userListaRole = userListaRole;
        this.userListaStatus = userListaStatus;
    }
}